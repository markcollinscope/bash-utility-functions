#!/bin/bash

source uts.global.shi
$(uts.return_if_read);

uts.source uts.base0.shi

rxcode.is()
{
	local ival=$(uts.int $1);
	$((ival >= 0)) && $((ival < 256));
}

rxcode.init()
{
	local ival=$(uts.val $1);
	assert rxcode.is $val;
	uts.yield $val;
}

rxcode()
{
	echo $1;
}

